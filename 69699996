local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

if not LocalPlayer then return end

pcall(function()
	local existing = LocalPlayer.PlayerGui:FindFirstChild("NewCustomMenu")
	if existing then existing:Destroy() end
end)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NewCustomMenu"
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

local function roundCorners(instance, radius)
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, radius)
	corner.Parent = instance
end

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Parent = ScreenGui
ToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
ToggleBtn.Position = UDim2.new(0.05, 0, 0.05, 0)
ToggleBtn.Size = UDim2.new(0, 110, 0, 30)
ToggleBtn.Text = "Menu"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.TextSize = 14
ToggleBtn.Font = Enum.Font.SourceSansBold
roundCorners(ToggleBtn, 8)

local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
MainFrame.Position = UDim2.new(0.05, 0, 0.12, 0)
MainFrame.Size = UDim2.new(0, 210, 0, 420)
MainFrame.Active = true
MainFrame.Draggable = true
roundCorners(MainFrame, 12)

ToggleBtn.MouseButton1Down:Connect(function()
	MainFrame.Visible = not MainFrame.Visible
end)

local Title = Instance.new("TextLabel")
Title.Parent = MainFrame
Title.BackgroundTransparency = 1
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Text = "CUSTOM MENU"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.Font = Enum.Font.SourceSansBold

local TabsContainer = Instance.new("Frame")
TabsContainer.Parent = MainFrame
TabsContainer.BackgroundTransparency = 1
TabsContainer.Position = UDim2.new(0, 5, 0, 35)
TabsContainer.Size = UDim2.new(1, -10, 0, 30)

local TabListLayout = Instance.new("UIListLayout")
TabListLayout.Parent = TabsContainer
TabListLayout.FillDirection = Enum.FillDirection.Horizontal
TabListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
TabListLayout.Padding = UDim.new(0, 4)

local containers = {}
local tabButtons = {}

local function createTab(name)
	local tabBtn = Instance.new("TextButton")
	tabBtn.Parent = TabsContainer
	tabBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
	tabBtn.Size = UDim2.new(0, 46, 0, 26)
	tabBtn.Text = name
	tabBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
	tabBtn.TextSize = 11
	tabBtn.Font = Enum.Font.SourceSansBold
	roundCorners(tabBtn, 6)

	local Container = Instance.new("ScrollingFrame")
	Container.Parent = MainFrame
	Container.BackgroundTransparency = 1
	Container.Position = UDim2.new(0, 0, 0, 70)
	Container.Size = UDim2.new(1, 0, 1, -70)
	Container.CanvasSize = UDim2.new(0, 0, 0, 0)
	Container.AutomaticCanvasSize = Enum.AutomaticSize.Y
	Container.ScrollBarThickness = 3
	Container.Visible = false

	local UIList = Instance.new("UIListLayout")
	UIList.Parent = Container
	UIList.HorizontalAlignment = Enum.HorizontalAlignment.Center
	UIList.SortOrder = Enum.SortOrder.LayoutOrder
	UIList.Padding = UDim.new(0, 8)

	local UIPadding = Instance.new("UIPadding")
	UIPadding.Parent = Container
	UIPadding.PaddingBottom = UDim.new(0, 15)
	UIPadding.PaddingTop = UDim.new(0, 5)

	containers[name] = Container
	tabButtons[name] = tabBtn

	tabBtn.MouseButton1Down:Connect(function()
		for _, c in pairs(containers) do c.Visible = false end
		for _, b in pairs(tabButtons) do 
			b.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
			b.TextColor3 = Color3.fromRGB(200, 200, 200)
		end
		Container.Visible = true
		tabBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
		tabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	end)

	return Container
end

local MainTab = createTab("Main")
local VisualsTab = createTab("Visuals")
local TrollTab = createTab("Troll")
local MM2Tab = createTab("MM2")

containers["Main"].Visible = true
tabButtons["Main"].BackgroundColor3 = Color3.fromRGB(80, 80, 80)
tabButtons["Main"].TextColor3 = Color3.fromRGB(255, 255, 255)

local function createToggle(tab, name, callback)
	local btn = Instance.new("TextButton")
	btn.Parent = tab
	btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
	btn.Size = UDim2.new(0, 185, 0, 40)
	btn.Text = name .. ": OFF"
	btn.TextColor3 = Color3.fromRGB(255, 100, 100)
	btn.TextSize = 12
	btn.Font = Enum.Font.SourceSansBold
	roundCorners(btn, 8)
	
	local state = false
	btn.MouseButton1Down:Connect(function()
		state = not state
		if state then
			btn.Text = name .. ": ON"
			btn.TextColor3 = Color3.fromRGB(100, 255, 100)
			btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
		else
			btn.Text = name .. ": OFF"
			btn.TextColor3 = Color3.fromRGB(255, 100, 100)
			btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
		end
		pcall(function() callback(state) end)
	end)
end

local aimEnabled, silentEnabled, hitboxesEnabled, flyEnabled, bhopEnabled, clickTpEnabled = false, false, false, false, false, false
local espEnabled, chamsEnabled, nametagsEnabled, fullbrightEnabled, fovEnabled, freecamEnabled, xrayEnabled = false, false, false, false, false, false, false
local invisEnabled, hiddenfling, spinbotEnabled, antiflingEnabled, sitanywhereEnabled, followplayerEnabled, attachtotargetEnabled = false, false, false, false, false, false, false
local mm2espEnabled = false
local tpwalking = false
local flingThread = nil
local currentBhopSpeed = 16

createToggle(MainTab, "Aim", function(s) aimEnabled = s end)
createToggle(MainTab, "Silent Aim", function(s) silentEnabled = s end)
createToggle(MainTab, "Hitboxes", function(s) hitboxesEnabled = s end)
createToggle(MainTab, "Fly (1.5)", function(s) flyEnabled = s end)
createToggle(MainTab, "Bunnyhop", function(s) bhopEnabled = s end)
createToggle(MainTab, "Click TP Tool", function(s)
	clickTpEnabled = s
	if s then
		local tool = Instance.new("Tool")
		tool.Name = "ClickTP Tool"
		tool.RequiresHandle = false
		tool.Activated:Connect(function()
			local mouse = LocalPlayer:GetMouse()
			if mouse.Target and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
				LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(mouse.Hit.Position + Vector3.new(0, 3, 0))
			end
		end)
		tool.Parent = LocalPlayer.Backpack
	else
		pcall(function() LocalPlayer.Backpack:FindFirstChild("ClickTP Tool"):Destroy() end)
	end
end)

createToggle(VisualsTab, "ESP", function(s) espEnabled = s end)
createToggle(VisualsTab, "Chams", function(s) chamsEnabled = s end)
createToggle(VisualsTab, "NameTags", function(s) nametagsEnabled = s end)
createToggle(VisualsTab, "Fullbright", function(s) fullbrightEnabled = s end)
createToggle(VisualsTab, "FOV Changer", function(s) fovEnabled = s end)
createToggle(VisualsTab, "Freecam", function(s) freecamEnabled = s end)
createToggle(VisualsTab, "X-Ray", function(s)
	xrayEnabled = s
	for _, part in ipairs(workspace:GetDescendants()) do
		if part:IsA("BasePart") and not part:IsDescendantOf(LocalPlayer.Character) then
			part.LocalTransparencyModifier = s and 0.5 or 0
		end
	end
end)

createToggle(TrollTab, "Invisible", function(s)
	invisEnabled = s
	pcall(function()
		for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do
			if part:IsA("BasePart") or part:IsA("Decal") then
				part.Transparency = s and 1 or (part.Name == "HumanoidRootPart" and 1 or 0)
			end
		end
	end)
end)
createToggle(TrollTab, "Fling", function(s)
	hiddenfling = s
	if s then
		flingThread = coroutine.create(function()
			while hiddenfling do
				RunService.Heartbeat:Wait()
				local c = LocalPlayer.Character
				local hrp = c and c:FindFirstChild("HumanoidRootPart")
				if hrp then
					local vel = hrp.Velocity
					hrp.Velocity = vel * 25000 + Vector3.new(0, 25000, 0)
					RunService.RenderStepped:Wait()
					hrp.Velocity = vel
				end
			end
		end)
		coroutine.resume(flingThread)
	end
end)
createToggle(TrollTab, "SpinBot", function(s) spinbotEnabled = s end)
createToggle(TrollTab, "Anti-Fling", function(s)
	antiflingEnabled = s
	pcall(function()
		local hrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
		if hrp then hrp.CustomPhysicalProperties = s and PhysicalProperties.new(math.huge, 0, 0, math.huge, math.huge) or nil end
	end)
end)
createToggle(TrollTab, "Sit Anywhere", function(s)
	sitanywhereEnabled = s
	pcall(function()
		local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
		if hum then hum:ChangeState(s and Enum.HumanoidStateType.Seated or Enum.HumanoidStateType.Running) end
	end)
end)
createToggle(TrollTab, "Follow Player", function(s) followplayerEnabled = s end)
createToggle(TrollTab, "Attach to Player", function(s) attachtotargetEnabled = s end)

createToggle(MM2Tab, "MM2 Role ESP", function(s) mm2espEnabled = s end)

RunService.RenderStepped:Connect(function()
	if aimEnabled then
		pcall(function()
			local closestPlayer, closestDist = nil, math.huge
			for _, p in ipairs(Players:GetPlayers()) do
				if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
					local hum = p.Character:FindFirstChildOfClass("Humanoid")
					if hum and hum.Health > 0 then
						local screenPoint, onScreen = Camera:WorldToScreenPoint(p.Character.Head.Position)
						if onScreen then
							local dist = (Vector2.new(screenPoint.X, screenPoint.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
							if dist < closestDist then closestDist, closestPlayer = dist, p end
						end
					end
				end
			end
			if closestPlayer then Camera.CFrame = CFrame.new(Camera.CFrame.Position, closestPlayer.Character.Head.Position) end
		end)
	end

	if fovEnabled then Camera.FieldOfView = 100 else Camera.FieldOfView = 70 end

	if spinbotEnabled then
		pcall(function()
			local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
			if hrp then hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(50), 0) end
		end)
	end
end)

RunService.Heartbeat:Connect(function()
	if hitboxesEnabled then
		pcall(function()
			for _, p in ipairs(Players:GetPlayers()) do
				if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
					local hrp = p.Character.HumanoidRootPart
					hrp.Size = Vector3.new(5, 5, 5)
					hrp.Transparency = 0.7
					hrp.CanCollide = false
				end
			end
		end)
	end

	if bhopEnabled then
		pcall(function()
			local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
			if hum and hum.FloorMaterial ~= Enum.Material.Air then
				hum:ChangeState(Enum.HumanoidStateType.Jumping)
				currentBhopSpeed = math.min(48, currentBhopSpeed + 4)
				hum.WalkSpeed = currentBhopSpeed
			end
		end)
	end

	if followplayerEnabled or attachtotargetEnabled then
		pcall(function()
			local target, minDist = nil, math.huge
			for _, p in ipairs(Players:GetPlayers()) do
				if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
					local dist = (p.Character.HumanoidRootPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
					if dist < minDist then minDist, target = dist, p end
				end
			end
			if target and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
				local myHrp = LocalPlayer.Character.HumanoidRootPart
				local tHrp = target.Character.HumanoidRootPart
				if attachtotargetEnabled then
					myHrp.CFrame = tHrp.CFrame * CFrame.new(0, 3, 2)
				elseif followplayerEnabled then
					LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):MoveTo(tHrp.Position)
				end
			end
		end)
	end
end)

RunService.RenderStepped:Connect(function()
	for _, p in ipairs(Players:GetPlayers()) do
		if p ~= LocalPlayer and p.Character then
			local char = p.Character
			local hl = char:FindFirstChild("CustomChamsHL")
			if chamsEnabled then
				if not hl then
					hl = Instance.new("Highlight")
					hl.Name = "CustomChamsHL"
					hl.Adornee = char
					hl.FillColor = Color3.fromRGB(0, 255, 255)
					hl.OutlineColor = Color3.fromRGB(255, 255, 255)
					hl.Parent = char
				end
			else
				if hl then hl:Destroy() end
			end
		end
	end
end)

RunService.RenderStepped:Connect(function()
	for _, p in ipairs(Players:GetPlayers()) do
		if p ~= LocalPlayer and p.Character then
			local char = p.Character
			local hl = char:FindFirstChild("MM2EspHL")
			if mm2espEnabled then
				local role = "Innocent"
				local function check(itemContainer)
					if not itemContainer then return end
					for _, item in ipairs(itemContainer:GetChildren()) do
						if item:IsA("Tool") then
							local n = item.Name:lower()
							if n:find("knife") or n:find("blade") or n:find("dagger") then
								role = "Murderer"
							elseif n:find("gun") or n:find("revolver") or n:find("sheriff") then
								role = "Sheriff"
							end
						end
					end
				end
				check(char)
				if p:FindFirstChild("Backpack") then check(p.Backpack) end

				local col = Color3.fromRGB(0, 255, 0)
				if role == "Murderer" then col = Color3.fromRGB(255, 0, 0)
				elseif role == "Sheriff" then col = Color3.fromRGB(0, 100, 255) end

				if not hl then
					hl = Instance.new("Highlight")
					hl.Name = "MM2EspHL"
					hl.Adornee = char
					hl.Parent = char
				end
				hl.FillColor = col
				hl.OutlineColor = col
			else
				if hl then hl:Destroy() end
			end
		end
	end
end)

RunService.RenderStepped:Connect(function()
	for _, p in ipairs(Players:GetPlayers()) do
		if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
			local head = p.Character.Head
			local tag = head:FindFirstChild("CustomNameTagGui")
			if nametagsEnabled then
				if not tag then
					local bg = Instance.new("BillboardGui")
					bg.Name = "CustomNameTagGui"
					bg.Size = UDim2.new(0, 100, 0, 40)
					bg.StudsOffset = Vector3.new(0, 2.5, 0)
					bg.AlwaysOnTop = true
					bg.Parent = head
					
					local txt = Instance.new("TextLabel")
					txt.Size = UDim2.new(1, 0, 1, 0)
					txt.BackgroundTransparency = 1
					txt.Text = p.Name
					txt.TextColor3 = Color3.fromRGB(255, 255, 255)
					txt.TextStrokeTransparency = 0
					txt.TextSize = 14
					txt.Font = Enum.Font.SourceSansBold
					txt.Parent = bg
				end
			else
				if tag then tag:Destroy() end
			end
		end
	end
end)

RunService.RenderStepped:Connect(function()
	if fullbrightEnabled then
		Lighting.Brightness = 2
		Lighting.ClockTime = 14
		Lighting.GlobalShadows = false
	else
		Lighting.Brightness = 1
		Lighting.GlobalShadows = true
	end
end)

RunService.RenderStepped:Connect(function()
	if freecamEnabled then
		pcall(function()
			Camera.CameraType = Enum.CameraType.Scriptable
			local camCFrame = Camera.CFrame
			local speed = 1
			if UserInputService:IsKeyDown(Enum.KeyCode.W) then Camera.CFrame = camCFrame + (camCFrame.LookVector * speed) end
			if UserInputService:IsKeyDown(Enum.KeyCode.S) then Camera.CFrame = camCFrame - (camCFrame.LookVector * speed) end
			if UserInputService:IsKeyDown(Enum.KeyCode.A) then Camera.CFrame = camCFrame + (-camCFrame.RightVector * speed) end
			if UserInputService:IsKeyDown(Enum.KeyCode.D) then Camera.CFrame = camCFrame + (camCFrame.RightVector * speed) end
		end)
	else
		Camera.CameraType = Enum.CameraType.Custom
	end
end)

RunService.Heartbeat:Connect(function()
	if flyEnabled then
		pcall(function()
			local c = LocalPlayer.Character
			local h = c and c:FindFirstChildWhichIsA("Humanoid")
			if h and h.MoveDirection.Magnitude > 0 then
				c:TranslateBy(h.MoveDirection * 1.5)
			end
		end)
	end
end)
